//styles
import styles from './PhotoButton.module.scss';

const PhotoButton = () => {
    return (
        <button className={styles.button}>Смотреть фото</button>
    );
};

export default PhotoButton;